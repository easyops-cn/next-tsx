{
  "name": "@apps/test",
  "private": true,
  "version": "0.19.0",
  "type": "module",
  "scripts": {
    "start": "builder --watch",
    "build": "builder",
    "build:with-contracts": "builder --with-contracts"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{projectRoot}/storyboard.yaml"
        ]
      }
    }
  },
  "dependencies": {
    "@next-bricks/basic": "^1.62.1",
    "@next-bricks/containers": "^1.16.1",
    "@next-bricks/icons": "^1.6.1",
    "@next-tsx/builder": "^0.7.0",
    "@next-tsx/core": "^0.12.0"
  }
}
